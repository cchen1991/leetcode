# AGENTS.md

## Cursor Cloud specific instructions

This is a personal LeetCode solutions repository. It contains no runnable application, no services, no build system, and no external dependencies.

### Repository structure

- `README.md` — project title only
- Solution files are added over time as the user solves problems

### Development workflow

- **No dependencies to install.** There is no `package.json`, `requirements.txt`, or equivalent.
- **No build step.** Solutions are standalone files (language varies by user preference).
- **No tests, linting, or CI configured.** If the user adds a test framework or linter in the future, update this file accordingly.
- **Git** is the only required tool.
